package bot.data.combine;


public class Combine {

	public void combine() {

		/*
		 * String url = "C:\\Users\\kiwon\\Desktop\\개발툴\\Project\\국립국어원자료\\미팅.txt";
		 * TextFile txt = new TextFile(); String[] filterList = txt.excepteKorean(url);
		 * 
		 * String verbSQL = "SELECT WORD FROM DICTIONARY WHERE PART = '동사'"; String
		 * nounSQL = "SELECT WORD FROM DICTIONARY WHERE PART = '명사'";
		 * 
		 * DBselect verbSelect = new DBselect(verbSQL); String[] verbList =
		 * verbSelect.selectWord();
		 * 
		 * DBselect nounSelect = new DBselect(nounSQL); String[] nounList =
		 * nounSelect.selectWord();
		 * 
		 * for (int i = 0; i < verbList.length; i++) { System.out.println(nounList[i] +
		 * " " + verbList[i]); }
		 */
	}

}
